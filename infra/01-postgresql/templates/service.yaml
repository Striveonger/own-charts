apiVersion: v1
kind: Service
metadata:
  name: {{ include "01-postgresql.fullname" . }}
  labels:
    {{- include "01-postgresql.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "01-postgresql.selectorLabels" . | nindent 4 }}
